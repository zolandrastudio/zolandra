# SEO Strategy

## In scope
- Public marketing pages in `artifacts/zolandra-studio`
- The public home route (`/`) and its on-page sections
- Public SEO assets and metadata (`index.html`, `public/*`, routing, head tags)

## Out of scope
- Authenticated/internal application areas if added later
- Backend API endpoints in `artifacts/api-server` unless they directly serve public SEO assets
- Developer-only mockup sandbox artifacts

## Target audience
- Visitors evaluating Zolandra Studio for interior design and space-planning services.
- Current public marketing copy is Spanish-language.

## Primary keywords
- Unknown — likely brand + service intent terms, but not explicitly documented.

## Notes
- The current public site is a one-page Vite + React SPA in `artifacts/zolandra-studio`.
- The backend API does not currently serve public SEO assets such as `robots.txt`, `sitemap.xml`, or `llms.txt`.

## Dismissed categories
- (None yet)
